import { View, Text } from "react-native";
import React from "react";
import BookViewingScreen from "@/components/tenant/BookViewingScreen";

const BookViewing = () => {
  return <BookViewingScreen />;
};

export default BookViewing;
